Suil makes it possible to load a UI of any toolkit in a host using
any other toolkit. Hosts do not need to build against or link to foreign
toolkit libraries to use UIs written with that toolkit.

Suil currently supports every combination of Gtk 2, Qt 4, and X11

www: http://drobilla.net/software/suil/
