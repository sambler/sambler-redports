--- src/liboslquery/CMakeLists.txt.orig	2015-03-12 13:57:09 UTC
+++ src/liboslquery/CMakeLists.txt
@@ -6,16 +6,16 @@ INCLUDE_DIRECTORIES ( ../liboslexec )
 FLEX_BISON ( ../liboslexec/osolex.l ../liboslexec/osogram.y oso liboslquery_srcs compiler_headers )
 
 if (BUILDSTATIC)
-    ADD_LIBRARY ( oslquery STATIC ${liboslquery_srcs} )
+    ADD_LIBRARY ( oslquery15 STATIC ${liboslquery_srcs} )
 else ()
-    ADD_LIBRARY ( oslquery SHARED ${liboslquery_srcs} )
+    ADD_LIBRARY ( oslquery15 SHARED ${liboslquery_srcs} )
 endif ()
 
-TARGET_LINK_LIBRARIES ( oslquery ${OPENIMAGEIO_LIBRARY} ${Boost_LIBRARIES} )
-LINK_ILMBASE ( oslquery )
+TARGET_LINK_LIBRARIES ( oslquery15 ${OPENIMAGEIO_LIBRARY} ${Boost_LIBRARIES} )
+LINK_ILMBASE ( oslquery15 )
 
 if (BUILDSTATIC)
-    INSTALL ( TARGETS oslquery LIBRARY DESTINATION lib ARCHIVE DESTINATION lib)
+    INSTALL ( TARGETS oslquery15 LIBRARY DESTINATION lib ARCHIVE DESTINATION lib)
 else ()
-    INSTALL ( TARGETS oslquery LIBRARY DESTINATION lib )
+    INSTALL ( TARGETS oslquery15 LIBRARY DESTINATION lib )
 endif ()
