--- src/liboslcomp/CMakeLists.txt.orig	2014-03-11 18:07:38 UTC
+++ src/liboslcomp/CMakeLists.txt
@@ -14,18 +14,18 @@ FILE ( GLOB compiler_headers "*.h" )
 FLEX_BISON ( osllex.l oslgram.y osl liboslcomp_srcs compiler_headers )
 
 if (BUILDSTATIC)
-    ADD_LIBRARY ( oslcomp STATIC ${liboslcomp_srcs} )
+    ADD_LIBRARY ( oslcomp14 STATIC ${liboslcomp_srcs} )
     ADD_DEFINITIONS ( -DBUILD_STATIC=1 )
 else ()
-    ADD_LIBRARY ( oslcomp SHARED ${liboslcomp_srcs} )
+    ADD_LIBRARY ( oslcomp14 SHARED ${liboslcomp_srcs} )
 endif ()
 
-TARGET_LINK_LIBRARIES ( oslcomp ${OPENIMAGEIO_LIBRARY} ${Boost_LIBRARIES} ${CMAKE_DL_LIBS})
-LINK_ILMBASE ( oslcomp )
+TARGET_LINK_LIBRARIES ( oslcomp14 ${OPENIMAGEIO_LIBRARY} ${Boost_LIBRARIES} ${CMAKE_DL_LIBS})
+LINK_ILMBASE ( oslcomp14 )
 
 if (BUILDSTATIC)
-    INSTALL ( TARGETS oslcomp LIBRARY DESTINATION lib ARCHIVE DESTINATION lib)
+    INSTALL ( TARGETS oslcomp14 LIBRARY DESTINATION lib ARCHIVE DESTINATION lib)
 else ()
-    INSTALL ( TARGETS oslcomp LIBRARY DESTINATION lib )
+    INSTALL ( TARGETS oslcomp14 LIBRARY DESTINATION lib )
 endif ()
 
