--- src/iconvert/CMakeLists.txt.orig	2015-12-02 07:17:13 UTC
+++ src/iconvert/CMakeLists.txt
@@ -1,6 +1,6 @@
 set (iconvert_srcs iconvert.cpp)
-add_executable (iconvert ${iconvert_srcs})
-set_target_properties (iconvert PROPERTIES FOLDER "Tools")
-target_link_libraries (iconvert OpenImageIO ${Boost_LIBRARIES} ${CMAKE_DL_LIBS})
-link_ilmbase (iconvert)
-oiio_install_targets (iconvert)
+add_executable (iconvert15 ${iconvert_srcs})
+set_target_properties (iconvert15 PROPERTIES FOLDER "Tools")
+target_link_libraries (iconvert15 OpenImageIO15 ${Boost_LIBRARIES} ${CMAKE_DL_LIBS})
+link_ilmbase (iconvert15)
+oiio_install_targets (iconvert15)
