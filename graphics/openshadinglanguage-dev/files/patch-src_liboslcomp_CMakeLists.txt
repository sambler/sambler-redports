--- src/liboslcomp/CMakeLists.txt.orig	2018-05-30 23:02:32 UTC
+++ src/liboslcomp/CMakeLists.txt
@@ -14,25 +14,25 @@ FILE ( GLOB compiler_headers "*.h" )
 FLEX_BISON ( osllex.l oslgram.y osl liboslcomp_srcs compiler_headers )
 
 if (BUILDSTATIC)
-    ADD_LIBRARY ( oslcomp STATIC ${liboslcomp_srcs} )
+    ADD_LIBRARY ( oslcomp_dev STATIC ${liboslcomp_srcs} )
     ADD_DEFINITIONS ( -DBUILD_STATIC=1 )
 else ()
-    ADD_LIBRARY ( oslcomp SHARED ${liboslcomp_srcs} )
+    ADD_LIBRARY ( oslcomp_dev SHARED ${liboslcomp_srcs} )
 
     if (VERBOSE)
         message(STATUS "Setting SOVERSION to: ${SOVERSION}")
     endif ()
-    set_target_properties( oslcomp
+    set_target_properties( oslcomp_dev
                           PROPERTIES
                           VERSION ${OSL_VERSION_MAJOR}.${OSL_VERSION_MINOR}.${OSL_VERSION_PATCH}
                           SOVERSION ${SOVERSION} )
 
 endif ()
 
-TARGET_LINK_LIBRARIES ( oslcomp ${OPENIMAGEIO_LIBRARIES} ${ILMBASE_LIBRARIES}
+TARGET_LINK_LIBRARIES ( oslcomp_dev ${OPENIMAGEIO_LIBRARIES} ${ILMBASE_LIBRARIES}
                        ${Boost_LIBRARIES} ${CMAKE_DL_LIBS}
                        ${CLANG_LIBRARIES} ${LLVM_LIBRARIES} ${LLVM_LDFLAGS}
                        ${LLVM_SYSTEM_LIBRARIES})
 
-install_targets (oslcomp)
+install_targets (oslcomp_dev)
 
