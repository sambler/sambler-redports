--- src/liboslquery/CMakeLists.txt.orig	2018-05-30 23:02:32 UTC
+++ src/liboslquery/CMakeLists.txt
@@ -7,19 +7,19 @@ INCLUDE_DIRECTORIES ( ../liboslexec )
 FLEX_BISON ( ../liboslexec/osolex.l ../liboslexec/osogram.y oso liboslquery_srcs compiler_headers )
 
 if (BUILDSTATIC)
-    ADD_LIBRARY ( oslquery STATIC ${liboslquery_srcs} )
+    ADD_LIBRARY ( oslquery_dev STATIC ${liboslquery_srcs} )
 else ()
-    ADD_LIBRARY ( oslquery SHARED ${liboslquery_srcs} )
+    ADD_LIBRARY ( oslquery_dev SHARED ${liboslquery_srcs} )
 
     if (VERBOSE)
         message(STATUS "Setting SOVERSION to: ${SOVERSION}")
     endif ()
-    set_target_properties( oslquery
+    set_target_properties( oslquery_dev
                           PROPERTIES
                           VERSION ${OSL_VERSION_MAJOR}.${OSL_VERSION_MINOR}.${OSL_VERSION_PATCH}
                           SOVERSION ${SOVERSION} )
 endif ()
 
-TARGET_LINK_LIBRARIES ( oslquery ${OPENIMAGEIO_LIBRARIES} ${ILMBASE_LIBRARIES} ${Boost_LIBRARIES} )
+TARGET_LINK_LIBRARIES ( oslquery_dev ${OPENIMAGEIO_LIBRARIES} ${ILMBASE_LIBRARIES} ${Boost_LIBRARIES} )
 
-install_targets (oslquery)
+install_targets (oslquery_dev)
